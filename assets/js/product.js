document.addEventListener('DOMContentLoaded',async()=>{const root=document.getElementById('product-root');try{const id=new URLSearchParams(location.search).get('id');const product=(await loadProducts()).find(item=>item.id===id&&item.visible!==false);if(!product)throw new Error('Товар не найден');document.title=`${product.name} — Настюшины игрушки`;const images=product.images||[];const stock=Number(product.stock)||0;root.innerHTML=`<a class="back-link" href="index.html#catalog">← вернуться к игрушкам</a><div class="product-layout"><section><div class="gallery-main" id="gallery-main">${images.length?`<img src="${safeText(images[0])}" alt="${safeText(product.name)}">`:`<div class="product-large-placeholder">${safeText(product.emoji||'♡')}</div>`}</div>${images.length>1?`<div class="gallery-thumbs">${images.map((image,index)=>`<button class="gallery-thumb ${index===0?'active':''}" data-image="${safeText(image)}" type="button"><img src="${safeText(image)}" alt="Фото ${index+1}"></button>`).join('')}</div>`:''}</section><section class="product-info">${product.badge?`<span class="product-badge">${safeText(product.badge)}</span>`:''}<h1>${safeText(product.name)}</h1><p class="lead">${safeText(product.description)}</p>${product.details?.length?`<ul class="detail-list">${product.details.map(item=>`<li>${safeText(item)}</li>`).join('')}</ul>`:''}<div class="buy-row"><span class="big-price">${money(product.price)}</span><span class="stock-note">${stock>0?`осталось: ${stock} шт.`:'нет в наличии'}</span></div><button class="add-to-cart" id="add-to-cart" type="button" ${stock<1?'disabled':''}>${stock>0?'положить в корзинку ♡':'скоро вернётся'}</button></section></div>`;root.querySelectorAll('.gallery-thumb').forEach(button=>button.addEventListener('click',()=>{root.querySelector('#gallery-main img').src=button.dataset.image;root.querySelectorAll('.gallery-thumb').forEach(item=>item.classList.remove('active'));button.classList.add('active')}));document.getElementById('add-to-cart')?.addEventListener('click',()=>addToCart(product.id))}catch(error){console.error(error);root.innerHTML='<div class="empty-cart"><div class="empty-icon">♡</div><h1 class="page-title">Игрушка спряталась</h1><p>Возможно, её уже забрали домой или ссылка устарела.</p><a class="button primary" href="index.html#catalog">в каталог</a></div>'}});
